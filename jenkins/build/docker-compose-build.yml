version: "3.1"

services:
        nginx:
                image: "ildiesign-nginx:latest"
                build:
                        context: ../../nginx/

        website:
                image: "ildiesign-node:latest"
                build:
                        context: ../../

